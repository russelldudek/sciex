
const navToggle=document.querySelector('.nav-toggle');const nav=document.querySelector('#site-nav');if(navToggle&&nav){navToggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});}
const scenarios={service:{signal:'Remote support knowledge is abundant but unevenly reusable.',decision:'Resolve the workflow, expert authority and first-time-resolution evidence before scaling.',state:'Service method'},commercial:{signal:'Quote-to-cash decisions cross multiple owners and systems.',decision:'Resolve ownership, handoff evidence and adoption in the commercial workflow.',state:'Commercial method'},operations:{signal:'Machine, production and issue signals compete for attention.',decision:'Resolve exception priority, daily management and measurable flow response.',state:'Operations method'},quality:{signal:'Review work can be accelerated only if quality authority remains explicit.',decision:'Resolve evidence, exception boundaries and QARA ownership before standardization.',state:'Quality method'}};
const buttons=[...document.querySelectorAll('.scenario')];const shell=document.querySelector('.spectrum-shell');const signal=document.querySelector('#scenario-signal');const decision=document.querySelector('#scenario-decision');const state=document.querySelector('#spectrum-state');buttons.forEach(btn=>btn.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const s=scenarios[btn.dataset.scenario];if(signal)signal.textContent=s.signal;if(decision)decision.textContent=s.decision;if(state)state.textContent=s.state;if(shell){shell.classList.remove('resolved');requestAnimationFrame(()=>shell.classList.add('resolved'));}}));setTimeout(()=>shell&&shell.classList.add('resolved'),900);
